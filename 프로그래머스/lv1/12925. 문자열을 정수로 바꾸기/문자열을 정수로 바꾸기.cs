public class Solution {
    public int solution(string s) {
        int answer = 0;

        answer = int.Parse(s);

        // for (int i = 0; i < s.Length; i++)
        // {
        //     if (s[i] == '-')
        //     {
        //         answer *= -1;
        //     }
        // }
        
        return answer;
    }
}